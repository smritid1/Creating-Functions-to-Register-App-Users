# Input Validation Project 

## Overview
This project implements basic input validation functions in Python. It ensures that user-provided **names** and **emails** meet specific criteria before being accepted.


## Objectives

- Validate user name input
- Validate user email format
- Return boolean results (`True` / `False`) based on validation rules

